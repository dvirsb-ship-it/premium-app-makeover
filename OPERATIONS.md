# JustAsk — ספר הפעלה

מה לעשות כשמשהו קורה, ואיפה כל דבר יושב. נכתב 29/07/2026.

---

## הדבר הראשון לבדוק כשמשהו לא עובד

**המשרד הטכנולוגי → סעיף "תקלות שרת"** (`/admin/verifications`).

כל כשל בפונקציות ה־AI נרשם שם אוטומטית עם ההודעה המדויקת. זה המקום היחיד
שבו רואים שגיאות שרת — הן **לא** מגיעות ל־Cloud Logging, והמשתמש רואה רק
"הייתה תקלה רגעית בחיבור".

אם הסעיף ריק אבל משהו בכל זאת שבור — הבעיה בצד הלקוח (הדפדפן), לא בשרת.

---

## התראות שמגיעות אליך מעצמן

| מה | לאן | מתי |
|---|---|---|
| האתר נפל | justask.adv@gmail.com | תוך דקה |
| ההוצאה עברה 50% / 90% / 100% מ־₪25 בחודש | מייל החיוב + justask.adv | יומי |

אם לא קיבלת מייל — האתר עולה והעלות בסדר.

---

## איפה הכול יושב

- **הקוד**: github.com/dvirsb-ship-it/premium-app-makeover, ענף `main`
- **פריסה**: דחיפה ל־main מפעילה פריסה אוטומטית. לוקח ~4 דקות.
  מעקב: Firebase Console → App Hosting → justask → Rollouts
- **האתר החי**: https://justask--justask-6bfb9.europe-west4.hosted.app
- **מסד נתונים**: Firestore, אזור me-west1 (תל אביב)
- **גיבוי**: Point-in-Time Recovery פעיל — אפשר לשחזר לכל רגע ב־7 הימים האחרונים
- **מפתח Gemini**: Secret Manager, בשם `GEMINI_API_KEY`

---

## שני דברים שישברו אותך אם תשכח אותם

### 1. בנייה מקומית אינה מוכיחה כלום

`bun run build` רגיל מייצר בנייה של **Cloudflare**, לא של הענן. הוא יעבור
בהצלחה גם על קוד שקורס בפרודקשן.

לבדיקה אמיתית לפני דחיפה:

```bash
NITRO_PRESET=node-server bun run build
```

### 2. חבילות npm כבדות עלולות להישבר באריזה

זה כבר קרה פעמיים: `@sentry/cloudflare` ואחר כך `firebase-admin`. שתיהן
עברו בנייה בהצלחה וקרסו בזמן ריצה בענן.

**לכן**: גישות שרת מיוחסות נעשות ב־`fetch` נקי מול REST API, בלי SDK.
הקוד ב־`src/lib/ai/server-admin.ts`. אל תחזיר לשם את firebase-admin.

---

## פעולות נפוצות

### פרסום שינוי בחוקי המסד

עריכת `firestore.rules` בקוד **לא מספיקה** — צריך לפרסם ידנית:

1. העתקה: `LANG=en_US.UTF-8 pbcopy < firestore.rules`
   (ה־LANG חובה, אחרת העברית נהרסת)
2. Firebase Console → Firestore → Rules
3. לחיצה **על שורת קוד** בעורך, ואז `cmd+A` ו־`cmd+V`
4. Publish

אותו דבר ל־`storage.rules`. שם גם יקפוץ דיאלוג
"Provision cross-service rules" → צריך לאשר **Attach permissions**.

### הוספת אדמין

שלושה מקומות, כולם חייבים להתאים:
1. `src/lib/admin.ts` → `ADMIN_EMAILS`
2. `firestore.rules` → `isAdmin()`
3. `storage.rules` → `isAdmin()`

### שחזור מסד אחרי טעות

Firebase Console → Firestore → Disaster Recovery → Point-in-time recovery.
עד 7 ימים אחורה.

---

## החלטות שקיבלת ולמה

- **לקוח לא משלם. אף פעם.** את השירות מממנים עורכי הדין.
- **עו"ד חינם בחצי השנה הראשונה** (בניית קהל). אחריה: תשלום פר־חיבור
  שהבשיל, או מנוי Pro ‏249₪/חודש.
  ⚠️ **לפני הפעלת סליקה** — כללי האתיקה של לשכת עורכי הדין אוסרים דמי
  תיווך על הפניית לקוחות. תשלום פר־חיבור עלול להיחשב כך; מנוי בטוח יותר.
  שווה חוות דעת של עו"ד שמכיר את הכללים.
- **ה־AI הוא שומר סף אמיתי** — הוא חוסם מקרים בלי עילה ומפנה למקום הנכון,
  ולא רק אוסף פרטים. זה הבידול של המוצר; אל תרכך אותו כדי להגדיל נפח.
- **הוולידציה נכתבת מהשרת בלבד.** הלקוח לא יכול לשנות סטטוס תיק — אחרת
  אפשר היה לעקוף את שומר הסף.
- **עד יצירת חיבור אין פרטים מזהים**: שם פרטי בלבד, טלפונים מסוננים,
  ותמונות עוברות צנזור AI אוטומטי.
- **כל הבטחה באפליקציה חייבת להיות אמת.** אין "תוך שעתיים" אם אין מי
  שיענה, ואין "נעדכן אותך" על תיק שנדחה.
- **ההתאמה לפי תחום היא הבטחה, לא נוחות.** היא מופיעה ב-11 מקומות
  באפליקציה, אחד מהם במסך ההסכמה שהלקוח מסמן עליו וי. הסינון קורה
  בשני מקומות ושניהם חייבים להתקיים: `adminApprovedLawyerIds(category)`
  בשרת (למי נשלחת ההתראה) ו-`categoryMatchesSpecialties` ב-store
  (מה מופיע בפיד). שניהם נשענים על `src/lib/specialties.ts` — טבלה אחת,
  לא שתיים. הבאג הזה נולד בדיוק מזה שהיו שתיים.
- **רשימת התחומים כפופה למה שה-AI מסווג.** שבעה תחומים, לא יותר. תחום
  שאין לו קטגוריה תואמת בוולידציה הוא מלכודת: עורך הדין נרשם, עובר
  אימות, ולא מקבל אף פנייה לעולם. להוסיף תחום = לעדכן גם את הפרומפט
  של הוולידציה, גם את `CATEGORY_SPECS`, וגם את גבולות צ'אט הקליטה.
- **דירוג נכתב מהשרת בלבד** (`rateLawyerFn` → `ratings/{caseId}` →
  `lawyerStats`). מזהה המסמך הוא מזהה התיק, כך שדירוג אחד לכל חיבור.
  זה הנתון שהפירמידה של Pro תוכל להתבסס עליו — עד שהוא נבנה, `rating`
  היה מקובע ל-0 בקוד ולא היה יכול להשתנות לעולם.
- **אין מיון לפי מחיר.** מסך השוואת ההצעות מציג טווח ופיזור, ולא מכתיר
  "הכי זול". מיון לפי מחיר היה הופך את המדד לרשימת מחירים ומרוקן את
  ההטבה שמנוי אמור לקנות.

---

## מה עוד לא נבנה

- **התראות במייל.** יש פוש (ראו למטה), אבל מי שלא הפעיל אותו לא יקבל כלום.
  מייל היה מכסה את כולם — דורש חשבון SMTP וסיסמת אפליקציה.
- **סליקה** — לא נחוצה עד תום תקופת ההשקה.
- **התחברות עם Apple** — דורשת חשבון מפתח בתשלום. הכפתור מוסתר דרך
  `GOOGLE_ONLY` ב-`src/routes/auth.tsx`; שם גם מוסתרים אימייל וטלפון,
  שספקיהם לא הופעלו ב-Firebase Auth. להפעיל אותם = לשנות את הדגל *ואחרי*
  שהופעלו בקונסולה, אחרת הלחיצה מחזירה שגיאת Firebase גולמית.
- **דומיין משלך** — כרגע כתובת של Firebase.

---

## לפני שיווק ללקוחות

גייס קודם 5–10 עורכי דין מאומתים. תיק ראשון שיושב שבוע בלי מענה הורג
את הרושם הראשוני, ואי אפשר לתקן אותו.

---

## התראות דחיפה — איך זה עובד

- **הפעלה**: פרופיל → התראות → "התראות בפוש". דורש אישור של הדפדפן.
- **באייפון**: חייבים קודם להוסיף את האתר למסך הבית (שיתוף ← הוספה למסך
  הבית), אחרת אפל לא מאפשרת התראות בכלל. יש רמז בממשק.
- **מתי נשלחת**: כשהבדיקה המשפטית מסתיימת, וכשעו"ד מביע עניין בתיק.
- **כיבוי לפי נושא**: הכפתורים בדף ההתראות אמיתיים — הם נשמרים על המשתמש
  והשרת בודק אותם לפני כל שליחה.
- **אם לא מגיעות**: בדוק שההרשאה לא נחסמה בהגדרות הדפדפן, ושהטוקן קיים
  ב-Firestore תחת `users/{uid}.pushTokens`.

---

## מודל ה-Pro — מה הוכרע ומה כבר נבנה

**העיקרון:** Pro לא קונה מקום בתור. הוא קונה **גישה מוקדמת** — עורך דין Pro
רואה תיק חדש לפני האחרים. כך הלקוח רואה את כל ההצעות באותו סדר, ואנחנו
לא מנתבים אותו לפי מי שילם (מה שגם מחזק את אפיון דמי התיווך).

**סדר הדירוג בפיד:** התאמה (עיר + תחום) → חלון Pro → תגובתיות מדודה →
טריות. Pro הוא שובר שוויון, לא המפתח הראשי.

**מה שנבנה כבר, ועובד גם בתקופת החינם:**

1. **התזכיר המשפטי המלא** — 300–500 מילים של עילות, יסודותיהן, התיישנות,
   טענות נגד ומסלול. נוצר בכל ולידציה, וקודם היה נזרק. עכשיו נשמר ב־
   `cases/{id}/memo/full` ומוצג לעורך דין מאומת. חוסך לו את שעת העבודה
   הראשונה. בתת-אוסף כדי שנוכל להגביל ל-Pro בעתיד בלי מיגרציה.
2. **מדד תגובתיות שאנחנו מודדים** — לא הצהרה שיווקית. `validatedAt` על
   התיק, וכשעו"ד מציע אנחנו מודדים את הפער. הלקוח רואה "מגיב בתוך X".
   הכתיבה בשרת בלבד וחוקי המסד אוסרים כתיבה מהדפדפן — מדד שקוף חייב
   להיות כזה שאי אפשר לנפח.

**מה שהוחלט לא למכור:** נפח פניות בלי רלוונטיות, בלעדיות על תיק, הסתרת
שכר טרחה, ואחוזי הצלחה (לא ניתנים לאימות וגם מוקש אתי).
