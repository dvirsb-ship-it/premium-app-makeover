# שער ההשקה — מה חייב להיות דלוק לפני לקוחות אמיתיים

דברים שכובו **בכוונה** לצורך בדיקות ופיתוח, וחייבים לחזור לפני שהאפליקציה
מקבלת אנשים אמיתיים. הרשימה הזו קיימת כי "נזכור" הוא לא מנגנון.

---

## 1. מתגים שכבויים עכשיו

| מתג | קובץ | מצב היום | למה כבוי | מתי מחזירים |
|---|---|---|---|---|
| `OPEN_CASE_LIMIT_ENABLED` | `src/lib/limits.ts` | **false** | חסם את דביר מלבדוק ומלצלם מסכים לחנות | לפני ההשקה |
| `GOOGLE_ONLY` | `src/routes/auth.tsx` | **true** | התחברות אפל דורשת חשבון Apple Developer | כשיהיה חשבון — **חובה להגשה ל-App Store** |
| `AI_REDACTION_ENABLED` | `src/lib/ai/redact.ts` | **true** — הודלק 6/8/2026 | — | דלוק. אם תזכיר יוצא רדוד או שתיקים נפסלים בלי סיבה — חזרה ל-false |
| `AI_VERTEX_ENABLED` | `src/lib/ai/vertex.ts` | **false** | דורש הפעלת Vertex AI API בקונסולה והרשאת `aiplatform.user` לחשבון השירות — בלעדיהן כל קריאה נכשלת ב-403 | אחרי שדביר מפעיל את ה-API, ואז תיק בדיקה אחד |

**המכסה עצמה לא נמחקה** — כל המנגנון (ספירה בשרת, מחיקת תיק חסום,
התעלמות מתיקים תקועים, ההודעה במסך הבית) קיים ונבדק. רק השער פתוח.

## 2. לפני שמדליקים חזרה

- [ ] למחוק את תיקי הבדיקה מהחשבונות שלנו, אחרת נתחיל מעל המכסה
- [ ] לוודא ש-`bun run test` עובר (הבדיקות מתעדות את הערך הצפוי)
- [ ] לבדוק בטלפון: פתיחת פנייה רביעית מציגה את ההסבר **לפני** הראיון
- [ ] `AI_VERTEX_ENABLED` — להפעיל **Vertex AI API** בפרויקט
      `justask-6bfb9`, לוודא שחשבון השירות של App Hosting מקבל
      `roles/aiplatform.user`, ואז להדליק ולפתוח תיק בדיקה. עד אז
      הניתוח רץ בלי אזור נעוץ, וזה כתוב במדיניות הפרטיות
- [ ] `AI_REDACTION_ENABLED` דלוק — לפתוח תיק בדיקה ולוודא שהתזכיר
      יוצא באותה איכות. **מדיניות הפרטיות עדיין לא מבטיחה שמזהים אינם
      נשלחים** — לעדכן אותה רק אחרי שהתיק הזה יאושר כרגיל

## 3. עוד דברים שקשורים להשקה

- [ ] `minInstances: 1` ב-`apphosting.yaml` כשמתחילה תעבורה אמיתית
      (מבטל את ההמתנה הראשונה, ~40₪ לחודש)
- [ ] Cloud Scheduler ל-`/__cron/deletions` — אופציונלי, הסריקה כבר רצה
      מעצמה בכל בקשה
- [ ] חשבונות החנויות: Apple 99$/שנה, Google 25$ חד-פעמי
- [x] מפתח החתימה של אנדרואיד — נוצר, מגובה ב-iCloud ובשולחן העבודה

## 4. iOS — מה שחסום על חשבון Apple Developer

- [ ] **מפתח APNs (.p8)** — להוריד מ-Apple Developer ולהעלות ל-Firebase
      (Project Settings ← Cloud Messaging ← Apple app configuration).
      **בלעדיו התראות לא יגיעו לאייפון בכלל** — הקוד מוכן ומחכה לו.
- [ ] Bundle ID `il.co.justask.app` לרשום ב-Apple Developer
- [ ] Sign in with Apple — להפעיל ב-Firebase ולהוריד את `GOOGLE_ONLY`
- [ ] צוות חתימה ב-Xcode (Signing & Capabilities)

---

**הכלל:** כל מתג שמכובה לצורך פיתוח נרשם כאן באותו commit שבו כיבו אותו.
